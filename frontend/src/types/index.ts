import {
  QuizTableProps,
  QuizProps,
  MultipleChoiceProps,
  TrueFalseProps,
  QuizAddProps,
} from './type.quiz';

import {
  UserProps,
  UserQuizProps,
  LoginProps,
  LoginFormProps,
} from './type.user';

import { IForm, IQuestion } from './type.form';

export type {
  QuizTableProps,
  QuizProps,
  MultipleChoiceProps,
  TrueFalseProps,
  QuizAddProps,
  UserProps,
  UserQuizProps,
  LoginProps,
  LoginFormProps,
};

export { IForm, IQuestion };
